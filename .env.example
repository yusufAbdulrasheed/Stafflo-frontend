# Copy this to .env.local for local dev, or set in Vercel dashboard for production
# Replace with your actual backend URL (e.g. Railway, Render, etc.)
REACT_APP_API_URL=https://your-backend-url.railway.app/api
